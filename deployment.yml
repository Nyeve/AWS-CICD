apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone
  labels:
    app: castone
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: capstone
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: capstone
    spec:
      containers:
        - image: nyeve/capstone:latest
          imagePullPolicy: Always
          name: capstone
          ports:
            - containerPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: capstone
  labels:
    app: capstone
spec:
  type: LoadBalancer
  selector:
    app: capstone
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
      protocol: TCP